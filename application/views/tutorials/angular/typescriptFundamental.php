<div class="card">
  <div class="card-header">
    Typescript
  </div>
  <div class="card-body">
    <p class="card-text"></p>
    <pre class="prettyprint">
<table>
    <tr><td></td></tr>
    <tr><td>Typescript is a superset of javascript.</td></tr>
    <tr><td>In typescript we have the concept of strong typing or static typing, if you worked with java, .net if you define your variable we need to specify the type of that variable in typescript type is optional.</td></tr>
    <tr><td>by using strong typing we can makes our application more predictable and it also make easier to debug them when something goes wrong.</td></tr>
    <tr><td>Typescript also brings quite of few object oriented programming feature that we missed in javascript. It has the concept of classes, interfaces, constructors, access modifier like public and private, fields, property and generics and so on.</td></tr>
    <tr><td>Another benefit of typescript we can catch error at compile time instead of runtime but not all kinds of error but lot of errors.</td></tr>
    <tr><td>Great tooling</td></tr>
    <tr><td>Typescript compiler transpile to javascript that our browser understand.</td></tr>
</table>
    </pre>
  </div>
</div>